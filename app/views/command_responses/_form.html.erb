<%= form_for @command_response, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :response, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :response, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :device_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :device_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                command_responses_path, :class => 'btn' %>
  </div>
<% end %>
