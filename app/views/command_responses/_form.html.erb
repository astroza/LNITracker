<%= form_for(@command_response) do |f| %>
  <% if @command_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@command_response.errors.count, "error") %> prohibited this command_response from being saved:</h2>

      <ul>
      <% @command_response.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :response %><br>
    <%= f.text_field :response %>
  </div>
  <div class="field">
    <%= f.label :device_id %><br>
    <%= f.number_field :device_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
