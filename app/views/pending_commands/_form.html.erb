<%= form_for(@pending_command) do |f| %>
  <% if @pending_command.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pending_command.errors.count, "error") %> prohibited this pending_command from being saved:</h2>

      <ul>
      <% @pending_command.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :command %><br>
    <%= f.text_field :command %>
  </div>
  <div class="field">
    <%= f.label :device_id %><br>
    <%= f.number_field :device_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
